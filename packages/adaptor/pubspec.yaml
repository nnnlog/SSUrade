name: ssurade_adaptor

environment:
  sdk: ">=3.0.0 <4.0.0"

dependencies:
  flutter:
    sdk: flutter

  ssurade_application:
    path: ../application

  parallel_worker:
    path: ../parallel_worker

  injectable: ^2.4.4
  get_it: ^7.7.0
  path_provider: ^2.1.4
  flutter_secure_storage: ^9.2.2
  dart_scope_functions: ^0.1.8
  http: ^1.2.2
  flutter_inappwebview: ^6.0.0
  sentry_flutter: ^8.9.0
  html: ^0.15.4

dev_dependencies:
  build_runner: ^2.4.12
  auto_exporter: ^3.4.1
  injectable_generator: ^2.6.2
