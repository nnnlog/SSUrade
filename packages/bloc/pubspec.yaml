name: ssurade_bloc

environment:
  sdk: ">=3.7.0 <4.0.0"

dependencies:
  ssurade_application:
    path: ../application

  bloc: ^8.1.4
  copy_with_extension: ^6.0.1
  equatable: ^2.0.8

dev_dependencies:
  build_runner: ^2.5.4
  auto_exporter: ^3.5.1
  copy_with_extension_gen: ^6.0.1
